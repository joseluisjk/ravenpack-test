const endpoint = {
  ENDPOINT: 'https://jsonplaceholder.typicode.com'
}

const apiEndpoints = {
  USERS: `${endpoint.ENDPOINT}/users`,
  POSTS: `${endpoint.ENDPOINT}/posts`,
};

export default apiEndpoints;